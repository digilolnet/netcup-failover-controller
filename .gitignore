# Vendored AI agent skills: reproduced from skills-lock.json via
# `bunx skills add`; only the lockfile and the agent/ module fence are tracked.
.agents/
.claude/skills/
agent/*
!agent/go.mod

# Local build artifacts
bin/
